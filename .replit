language = "python3"

[deployment]
build = ["bash", "/home/runner/workspace/build.sh"]
run = ["python3", "/home/runner/workspace/main.py"]
deploymentTarget = "cloudrun"

[agent]
expertMode = true

# Explicitly declare port 5000
[ports]
5000 = { visibility = "public", description = "Flask webhook server" }
